import {memo, useEffect} from 'react';import PropTypes from 'prop-types';import {cn as bem} from '@bem-react/classname';import './style.css';import {useParams} from 'react-router-dom';import useStore from '../../store/use-store';import useSelector from '../../store/use-selector';import useTranslate from '../../store/use-translate';import Determination from '../../components/determinaton';function Product(props) {  const {id} = useParams()  const store = useStore();  const translate = useTranslate()  const select = useSelector(state => ({    product: state.catalog.product,  }));    useEffect(() => {    store.actions.catalog.loadProduct(id);    return () => store.actions.catalog.deleteProduct()  }, [id]);    const callbacks = {    onAdd: (e) => props.onAdd(id)  };    const cn = bem('Product');  return (        <div className={cn()}>      {!select?.product ? translate('Загрузка') + '...' :        <>          <p className={cn('description')}>{select?.product?.description}</p>          <Determination title={translate('Страна производитель')} description={select?.product?.madeIn?.title}/>          <Determination title={translate('Категория')} description={select?.product?.category?.title}/>          <Determination title={translate('Год выпуска')} description={select?.product?.edition}/>          <p className={cn('price')}>{translate('Цена')}: {select?.product?.price} ₽</p>          <button onClick={callbacks.onAdd}>{translate('Добавить')}</button>        </>      }    </div>  );}export default memo(Product);